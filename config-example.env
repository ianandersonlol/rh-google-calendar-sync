# Raid Helper API Key (get this from /usersettings apikey command in Discord)
RAID_HELPER_API_KEY=your_raid_helper_api_key_here

# Google Calendar ID
# Options:
#   'primary' - Use your main calendar
#   'separate' - Create/use a dedicated "Raid Helper Events" calendar (recommended - you can give it its own color!)
#   Or use a specific calendar ID from Google Calendar settings
GOOGLE_CALENDAR_ID=separate

# Path to your Google Calendar credentials JSON file
GOOGLE_CREDENTIALS_PATH=./credentials.json

# Path to store token for authenticated session
GOOGLE_TOKEN_PATH=./token.json
